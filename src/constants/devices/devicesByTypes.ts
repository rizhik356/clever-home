import { DevicesTypes } from './Types/DevicesData.ts'

const devicesByTypes: DevicesTypes = {
  SWITCH: {
    powerBtn: true,
  },
  SWITCH_HUB: {
    hasModal: true,
  },
}

export default devicesByTypes
